// Time (ms) it takes to move in or out
export let MOVE_DURATION = 800;

// Time (ms) the SVG pauses on screen
export let PAUSE_DURATION = 600;

// Starting off-screen position (vw / vh)
export let START_POSITION = {
  x: -20, // vw
  y: -20, // vh
};

// On-screen position (vw / vh)
export let IN_POSITION = {
  x: 10, // vw
  y: 10, // vh
};

// Ending off-screen position (vw / vh)
export let END_POSITION = {
  x: -20, // vw
  y: -20, // vh
};
