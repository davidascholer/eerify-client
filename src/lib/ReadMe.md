# About
- Hooks, components, and functions that override or wrap respective libraries.
- As much of the libraries' implementation as possible should be abstracted into each corresponding folder to limit work when updating dependencies.